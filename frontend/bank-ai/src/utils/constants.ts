/**
 * 全局常量定义
 * 集中管理所有硬编码字符串、枚举值和配置项
 */

// ---------------------------------------------------------------------------
// API 相关
// ---------------------------------------------------------------------------
export const DEFAULT_API_BASE_URL = 'http://127.0.0.1:8000'

export const API_BASE_URL =
  import.meta.env.VITE_API_BASE_URL || DEFAULT_API_BASE_URL

// ---------------------------------------------------------------------------
// 本地存储键名
// ---------------------------------------------------------------------------
export const TOKEN_KEY = 'bank_ai_token'

// ---------------------------------------------------------------------------
// 任务状态枚举
// ---------------------------------------------------------------------------
export enum TaskStatus {
  PENDING = 'pending',
  PROCESSING = 'processing',
  COMPLETED = 'completed',
  FAILED = 'failed',
}

// ---------------------------------------------------------------------------
// 风险等级枚举
// ---------------------------------------------------------------------------
export enum RiskLevel {
  HIGH = 'high',
  MEDIUM = 'medium',
  LOW = 'low',
  SAFE = 'safe',
  UNKNOWN = 'unknown',
}

// ---------------------------------------------------------------------------
// 用户状态枚举
// ---------------------------------------------------------------------------
export enum UserStatus {
  PENDING = 'pending',
  ACTIVE = 'active',
  DISABLED = 'disabled',
}

// ---------------------------------------------------------------------------
// 分页默认值
// ---------------------------------------------------------------------------
export const DEFAULT_PAGE = 1
export const DEFAULT_PAGE_SIZE = 10
export const DASHBOARD_RECORDS_LIMIT = 5

// ---------------------------------------------------------------------------
// 处理模式
// ---------------------------------------------------------------------------
export enum ProcessMode {
  DIRECT = 'DIRECT',
  RAG = 'RAG',
}

// ---------------------------------------------------------------------------
// 响应式断点
// ---------------------------------------------------------------------------
/** 移动端断点（px）。CSS @media 中也使用此值，修改时需同步 */
export const MOBILE_BREAKPOINT = 768
